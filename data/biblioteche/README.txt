Copy raw text files of "the Cathedral and the Bazaar" into ./raw/redwoods/cb
Corpus database files will be generated here automatically (e.g. redwoods.db)

Notes:
    symlink lexrules.db into this folder to browse as a corpus
